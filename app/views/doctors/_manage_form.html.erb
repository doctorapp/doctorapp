<% unless current_user?(@doctor) %>
	<div id="manage_form">
	<% if current_user.manage_approved?(@doctor) %>
		<%= render 'unmanage' %>
	<% elsif current_user.manage?(@doctor) %>
		<%= render 'pending' %>
	<% else %>
		<%= render 'manage' %>
	<% end %>
	</div>
<% end %>
