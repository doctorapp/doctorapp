<%= form_for(current_user.favorite_doctors.build(doctor_id: @doctor.id), remote: true)  do |f| %>
	<div><%= f.hidden_field :doctor_id %></div>
	<%= f.submit "favorite", class: "btn btn-large btn-primary" %>
<% end %>
